/*function goToCoffeeShop(drink, pastry) {
  alert(`Hey! Got your ${drink} and ${pastry}, headed your way!`);
}

goToCoffeeShop('Hot Mocha', 'Brownie Cluster');*/

function getArea(width, length, unit) {
  const area = width * length;
  return `${area} ${unit}`;
}

